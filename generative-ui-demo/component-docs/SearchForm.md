# SearchForm

搜索表单（基于 Arco Form），内联布局，用于筛选条件输入。

## Props

- `fields` (array of string): 字段名列表，每个字段渲染为 Input

## 示例

```json
{
  "component": "SearchForm",
  "props": {
    "fields": ["dateRange", "category", "region"]
  }
}
```
